---
title: Generating NONMEM monitor config file
description: A guide for generate_nmm_config.
pagefind: true
---

This guide describes how to create a [NONMEMmonitor](https://github.com/a2-ai/nonmem-monitor) (`nmm`) configuration `toml` file using the `generate_nmm_config` function. `nmm` is a NONMEM job submitter, that uses `bbi` to submit jobs, but includes some additional monitoring 