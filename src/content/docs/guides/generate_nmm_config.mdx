---
title: Generating NONMEM monitor config file
description: A guide for generate_nmm_config.
pagefind: true
---

import { Aside, Code, Steps } from '@astrojs/starlight/components';

This guide describes how to create a [NONMEMmonitor](https://github.com/a2-ai/nonmem-monitor) (`nmm`) configuration `toml` file using the `generate_nmm_config` function. 

## Prerequisites
<Steps>
1. `nmm` installed
2. [`nmm` specific template file](/blogs/nmm_template)
</Steps>

## Generating a `nmm` config
Here's the code you'll use to generate a `config.toml` file.

import generate_nmm from '/src/code/generate_nmm.R?raw';

<Code lang='r' title='generate_nmm_config.R' code={generate_nmm}/>

<Aside type='note'>
To understand what the [`watched_dir` argument](/reference/generate_nmm_config_reference#using-the-watched_dir-argument) and [`output_dir` argument](/reference/generate_nmm_config_reference#using-the-output_dir-argument) are doing, see the [`generate_nmm_config` reference guide](/reference/generate_nmm_config_reference)
</Aside>

This function call will generate a `1001.toml` file with the following contents
<Code lang='toml' title='1001.toml' 
code="model_number = '1001'
watched_dir = '/cluster-data/user-homes/user/slurmtools/model/nonmem'
output_dir = '/cluster-data/user-homes/user/slurmtools/model/nonmem/in_progress'"
/>

## Submitting a NONMEM job
You can then use [`submit_slurm_job`](/guides/submit_slurm_job) as normal!

## Next steps
For more advanced usage: If you’re interested in learning how to use non-default arguments with submit_slurm_job for more customized job submissions, please check out our [`generate_nmm_config` Reference Guide](/reference/generate_nmm_config_reference)! If you'd like to integrate notifications with `nmm` please see the [`nmm` alerter configuration guide](/blogs/using_alerter_with_nmm)
